import React from 'react';
import { Typography } from '@material-ui/core';

const WeatherInitialInfo: React.FC = () => {
  return (
    <Typography component="div">
      <h2>Hello!</h2>
      <p>Please agree so we could check your localization :)</p>
    </Typography>
  );
};

export default WeatherInitialInfo;
